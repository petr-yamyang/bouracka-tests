# INT-006 — Azure Static Web Apps outage-announcements feed

> **Discovered.** 2026-05-06 live-walk via Claude in Chrome on
> `demo.bouracka.cz/formular/`. Not in pre-existing INT-001..005 catalog.

## Identity

| Field | Value |
|-------|-------|
| ID | INT-006 |
| Name CS | Oznamovací kanál odstávek |
| Name EN | Outage-announcements feed |
| Provider | Microsoft Azure — Storage Static Website (`*.z6.web.core.windows.net`) |
| Endpoint | `https://bourackaodstavky78861.z6.web.core.windows.net/odstavky.json` |
| Method | `GET` (idempotent) |
| Auth | none (public Azure Storage with CORS) |
| Discovered on env | DEMO (`demo.bouracka.cz`) |
| Probable PROD form | identical (SPA bundle hard-codes the URL; Δ unlikely) |

## Behaviour

- The SPA fetches this JSON on **every route mount** (observed
  multiple GETs across rozcestník + intro + phase-1 navigation).
- Drives the orange "DEMO VERZE" persistent banner OR — more
  generally — any ČKP-side scheduled-outage announcement.
- 200-byte-class response (lightweight).
- Cache headers TBC (capture in next walk).

## Test relevance

| TT impact | TC impact |
|-----------|-----------|
| TT-CP-OUTAGE — outage-banner rendering | TC-CP-NEW-L (proposed): assert outage feed reachable; assert banner state matches feed contents |

## Failure modes worth asserting

1. **Feed unreachable** (Azure outage / DNS fail / CORS misconfig):
   does the SPA crash, render with stale state, or render without banner?
2. **Feed returns malformed JSON**: graceful degradation?
3. **Feed forces a maintenance state**: does the SPA gate
   the `VYPLNIT ZÁZNAM` CTA?

## Capture pending

- Fetch the actual JSON contents (next session, when egress allowlist
  includes `*.z6.web.core.windows.net` OR via Chrome JS inspector).
- Document JSON schema once captured.

## Status

| Item | Value |
|------|-------|
| Status | v0.1 — discovered; schema TBC |
| First seen | 2026-05-06 live-walk |
| Catalog row | INT-006 |
